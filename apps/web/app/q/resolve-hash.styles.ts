/** Publiczna strona rozwiązywania skanu QR po hash (Tailwind). */
export const resolveHashStyles = {
  main: 'mx-auto w-full max-w-[760px] px-4 py-10 font-sans text-neutral-900',
  heading: 'mb-2 text-2xl font-semibold text-neutral-900',
  errorAlert: 'text-sm font-medium text-red-600',
  bodyText: 'text-sm text-neutral-800',
  labelImage: 'w-full max-w-[400px] rounded border border-neutral-300 bg-white',
} as const;
